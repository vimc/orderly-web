orderly.root=${orderly_root}
db.template=${db_location}
db.location=${db_test_location}

# In seconds
token.lifespan=${token_lifespan}
token.issuer=${token_issuer}

app.version=${app_version}
app.port=${app_port}